TELEGRAM_BOT_TOKEN=your_bot_token
BOT_ID=you_bot_username
ALLOWED_USER_ID=numeric_telegram_id # the `daemon.py` script will return this in your first error if you can't find it on Telegram
FILE_PATH=path/to/repo

# LLM for brainstorm chat — set one (HF_TOKEN is used as fallback if present)
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini        # default
# HF_MODEL=meta-llama/Meta-Llama-3.1-8B-Instruct  # default when using HF_TOKEN
# HF_TOKEN=hf_...
